# Scribend

An offline-first medical scribe designed for healthcare workers in remote areas with zero Wi-Fi. It captures doctor-patient audio, converts it to text, retrieves historical patient context via local vector search, and structures the encounter into a JSON SOAP note—100% on-device.
